{
    "name": "digihum/LDAP",
    "description": "Adds LDAP to Omeka",
    "type": "library",
    "require": {
        "php": ">=5.6.0",
        "zendframework/zend-ldap": "^2.7.1"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "autoload-dev": {
        "classmap": [
            "src/"
        ]
    },
    "license": "MIT",
    "authors": [
        {
            "name": "OxCom",
            "email": "lancer.oxcom@gmail.com"
        }
    ],
    "scripts": {
        "test": [
            "composer install",
            "./vendor/bin/phpcs --standard=psr2 src/ tests/ZendTwig/ tests/Fixture/",
            "./vendor/bin/phpunit -c tests/"
        ]
    }
}
